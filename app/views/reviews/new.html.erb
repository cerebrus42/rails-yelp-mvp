<div class="container">
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating, input_html: { min: '0', max: '5', step: 'any' } %>
    <%= f.input :content %>
    <%= f.submit %>
  <% end %>
</div>
